"use strict";
const { httpsStatusCodes } = require("./http-status-codes");
const { httpResponses } = require("./http-responses");
const {
  success,
  failure,
} = require("./helper");

module.exports = {
  httpsStatusCodes,
  httpResponses,
  success,
  failure,
};
